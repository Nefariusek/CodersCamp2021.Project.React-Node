class Profile {
  constructor(userClass, name, age, medications) {
    this.userClass = userClass;
    this.userName = name;
    this.userAge = age;
    this.userMedications = medications;
  }
}

export default Profile;
